/**
 * Browser test setup — imported by vitest.browser.config.ts via test.setupFiles.
 * Loads the Mantine styles and app tokens so computed CSS values are real.
 */
import '@mantine/core/styles.css';
import '../tokens.css';
